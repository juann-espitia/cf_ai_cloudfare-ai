This document outlines the AI-assisted coding process to build Cloudflare-AI.

### 1. Architectural Guidance
> "How do I structure a Cloudflare Worker to handle both a standard fetch request and a class-based Workflow Entrypoint in the same file?"

### 2. State Persistence Logic
> "Provide a code snippet for retrieving and updating a JSON array in Cloudflare KV. I need to maintain a history of the last 10 messages for an AI chat session."

### 3. Workflow Implementation
> "What is the correct syntax for passing a 'userId' and 'message' from a Worker fetch handler into a Workflow instance to process data asynchronously?"
